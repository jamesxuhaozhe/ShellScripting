#!/bin/echo

function add
{
    echo `expr $1 + $2`
}

function reduce
{
    echo `expr $1 - $2`
}

function divide
{
    echo `expr $1 / $2`
}

function multiply
{
    echo `expr $1 \* $2`
}

function sys_info
{
    echo "Memory Info"
    echo
    free -m

    echo "Disk Info"
    echo
    df -h
}